# Campus Pay

This is a code bundle for Payment Gateway Interface. The original project is available at https://www.figma.com/design/Zyjwv3uet7GT33oCD9XWnC/Payment-Gateway-Interface.

## Running the code

Run `npm i` to install the dependencies.

Run `npm run dev` to start the development server.
